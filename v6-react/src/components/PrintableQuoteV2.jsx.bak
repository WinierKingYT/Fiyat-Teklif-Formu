import React from 'react';
import { translations } from '../utils/translations';

// Helper functions defined outside component to avoid recreation


const formatDate = (dateString, locale = 'tr-TR') => {
    if (!dateString) return '-';
    return new Date(dateString).toLocaleDateString(locale);
};

const PrintableQuote = ({
    id,
    theme = 'modern',
    color = '#000000',
    quoteData: _quoteData,
    customerData: _customerData,
    companyData: _companyData,
    bankData: _bankData,
    items: _items,
    discount: _discount,
    config: _config,
    layout,
    signature
}) => {
    const quoteData = React.useMemo(() => _quoteData || {}, [_quoteData]);
    const customerData = React.useMemo(() => _customerData || {}, [_customerData]);
    const companyData = React.useMemo(() => _companyData || {}, [_companyData]);
    const bankData = React.useMemo(() => _bankData || {}, [_bankData]);
    const items = React.useMemo(() => _items || [], [_items]);
    const discount = React.useMemo(() => _discount || {}, [_discount]);
    const hasLineItemDiscounts = React.useMemo(() => items.some(item => (item.discountRate || 0) > 0), [items]);

    const language = quoteData.language || 'tr';
    const t = translations[language] || translations['tr'];

    const localeMap = {
        'tr': 'tr-TR',
        'en': 'en-US',
        'de': 'de-DE'
    };
    const currentLocale = localeMap[language] || 'tr-TR';

    const formatCurrency = React.useCallback((amount) => {
        const currency = quoteData.currency || 'TRY';
        return new Intl.NumberFormat(currentLocale, { style: 'currency', currency: currency }).format(amount);
    }, [quoteData.currency, currentLocale]);

    // Default configuration
    const config = React.useMemo(() => ({
        showLogo: true,
        showBankInfo: true,
        showSignatures: true,
        showTerms: true,
        showNotes: true,
        showSummary: true,
        title: 'FİYAT TEKLİFİ',
        fontFamily: 'Inter',
        fontSize: 'medium',
        margins: 'normal',
        showTableImages: false,
        showTableUnit: true,
        showTableTax: true,
        showQRCode: false,
        qrCodeUrl: '',
        showWatermark: false,
        watermarkText: 'TASLAK',
        watermarkOpacity: 0.1,
        watermarkColor: '#000000',
        watermarkFontSize: 120,
        watermarkRotation: -45,
        customFooter: '',
        logoPosition: 'left',
        ...(_config || {})
    }), [_config]);

    const { subtotal, totalTax, total, discountAmount, taxBreakdown } = React.useMemo(() => {
        let subtotal = 0;
        let rawTotalTax = 0;
        const rawTaxBreakdown = {};

        items.forEach(item => {
            const total = (item.quantity || 0) * (item.price || 0);
            subtotal += total;
            const tax = total * ((item.taxRate || 0) / 100);
            rawTotalTax += tax;

            const rate = item.taxRate || 0;
            if (tax > 0) {
                rawTaxBreakdown[rate] = (rawTaxBreakdown[rate] || 0) + tax;
            }
        });

        let discountAmount = 0;
        let discountRatio = 0;

        if (subtotal > 0) {
            if (discount?.type === 'percentage') {
                discountAmount = subtotal * ((discount.value || 0) / 100);
                discountRatio = (discount.value || 0) / 100;
            } else if (discount?.type === 'fixed') {
                discountAmount = Number(discount.value) || 0;
                discountRatio = discountAmount / subtotal;
            }
        }

        // Ensure discount doesn't exceed subtotal
        if (discountAmount > subtotal) {
            discountAmount = subtotal;
            discountRatio = 1;
        }

        // Recalculate tax based on discounted amount
        const totalTax = rawTotalTax * (1 - discountRatio);

        const taxBreakdown = {};
        Object.keys(rawTaxBreakdown).forEach(rate => {
            taxBreakdown[rate] = rawTaxBreakdown[rate] * (1 - discountRatio);
        });

        const total = subtotal - discountAmount + totalTax;

        return { subtotal, totalTax, total, discountAmount, taxBreakdown };
    }, [items, discount]);

    const proStyles = React.useMemo(() => `
        .pro-theme-container {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            line-height: 1.4;
            color: #1e293b;
            background: white;
            padding: 0;
            max-width: 210mm;
            margin: 0 auto;
            font-size: 12px;
        }

        .pro-theme-container .pdf-preview {
            background: white;
            border-radius: 8px;
            padding: 1.5rem;
            position: relative;
            overflow: hidden;
        }

        .pro-theme-container .pdf-preview::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, ${color}, #ededed);
        }

        .pro-theme-container .pdf-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #e2e8f0;
        }

        .pro-theme-container .header-left {
            display: flex;
            align-items: flex-start;
            gap: 1.5rem;
            flex: 1;
        }

        .pro-theme-container .company-logo {
            width: 120px;
            height: 60px;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f8fafc;
            color: #000000;
            font-size: 0.6rem;
            text-align: center;
            padding: 0.5rem;
            overflow: hidden;
        }

        .pro-theme-container .company-logo img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
        }

        .pro-theme-container .company-info {
            flex: 1;
        }

        .pro-theme-container .company-name {
            font-size: 1rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 0.25rem;
        }

        .pro-theme-container .company-details {
            font-size: 0.7rem;
            color: #000000;
            line-height: 1.3;
        }

        .pro-theme-container .quote-info {
            text-align: right;
            background: #f8fafc;
            padding: 0.8rem;
            border-radius: 6px;
            border-left: 3px solid ${color};
            min-width: 200px;
        }

        .pro-theme-container .quote-title {
            font-size: 0.9rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }

        .pro-theme-container .quote-meta {
            font-size: 0.7rem;
            color: #64748b;
        }

        .pro-theme-container .quote-meta div {
            margin-bottom: 0.125rem;
        }

        .pro-theme-container .quote-number {
            font-weight: 600;
            color: #000000;
        }

        .pro-theme-container .customer-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .pro-theme-container .customer-box {
            background: #f8fafc;
            border-radius: 6px;
            padding: 0.8rem;
            border: 1px solid #e2e8f0;
        }

        .pro-theme-container .section-title {
            font-size: 0.8rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.375rem;
        }

        .pro-theme-container .info-grid {
            display: grid;
            gap: 0.3rem;
        }

        .pro-theme-container .info-line {
            display: flex;
            font-size: 0.7rem;
        }

        .pro-theme-container .info-label {
            font-weight: 500;
            color: #475569;
            min-width: 60px;
        }

        .pro-theme-container .info-value {
            color: #1e293b;
            flex: 1;
        }

        .pro-theme-container .pdf-items-table {
            width: 100%;
            border-collapse: collapse;
            margin: 1rem 0;
            font-size: 0.7rem;
            background: white;
            border-radius: 6px;
            overflow: hidden;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
        }

        .pro-theme-container .pdf-items-table th {
            background: #f1f5f9;
            padding: 0.5rem 0.3rem;
            text-align: left;
            font-weight: 600;
            color: #475569;
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border-bottom: 1px solid #e2e8f0;
        }

        .pro-theme-container .pdf-items-table td {
            padding: 0.4rem 0.3rem;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: top;
        }

        .pro-theme-container .pdf-items-table tr:last-child td {
            border-bottom: none;
        }

        .pro-theme-container .pdf-items-table tr:hover {
            background: #f8fafc;
        }

        .pro-theme-container .item-image {
            width: 40px;
            height: 40px;
            border-radius: 4px;
            object-fit: cover;
            background: #f1f5f9;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #94a3b8;
            font-size: 0.5rem;
            text-align: center;
            overflow: hidden;
        }

        .pro-theme-container .item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 4px;
        }

        .pro-theme-container .item-description {
            font-weight: 500;
            color: #1e293b;
        }

        .pro-theme-container .item-details {
            font-size: 0.6rem;
            color: #64748b;
            margin-top: 0.2rem;
            line-height: 1.2;
        }

        .pro-theme-container .item-price, .pro-theme-container .item-total {
            font-weight: 500;
            color: #1e293b;
            text-align: right;
        }

        .pro-theme-container .item-quantity, .pro-theme-container .item-tax, .pro-theme-container .item-unit {
            color: #64748b;
            text-align: center;
        }

        .pro-theme-container .pdf-summary {
            background: #f8fafc;
            border-radius: 6px;
            padding: 0.8rem;
            margin: 1rem 0;
            border: 1px solid #e2e8f0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .pro-theme-container .totals-section {
            background: white;
            border-radius: 6px;
            padding: 0.8rem;
            border: 1px solid #e2e8f0;
        }

        .pro-theme-container .summary-row {
            display: flex;
            justify-content: space-between;
            padding: 0.3rem 0;
            border-bottom: 1px solid #f1f5f9;
            font-size: 0.7rem;
        }

        .pro-theme-container .summary-row:last-child {
            border-bottom: none;
        }

        .pro-theme-container .summary-row.grand-total {
            font-weight: 700;
            font-size: 0.8rem;
            color: #1e293b;
            padding-top: 0.5rem;
            margin-top: 0.25rem;
            border-top: 2px solid #e2e8f0;
        }

        .pro-theme-container .payment-info {
            font-size: 0.7rem;
        }

        .pro-theme-container .payment-info h3 {
            font-size: 0.8rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }

        .pro-theme-container .bank-info {
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #e2e8f0;
        }

        .pro-theme-container .bank-info h3 {
            font-size: 0.8rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }

        .pro-theme-container .signature-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin: 1rem 0;
            padding-top: 1rem;
            border-top: 1px solid #e2e8f0;
        }

        .pro-theme-container .signature-box {
            text-align: center;
            padding: 0.8rem;
        }

        .pro-theme-container .signature-area {
            height: 60px;
            border-bottom: 1px solid #cbd5e1;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: flex-end;
            justify-content: center;
            position: relative;
        }

        .pro-theme-container .stamp-area {
            height: 60px;
            border: 1px dashed #cbd5e1;
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #94a3b8;
            font-size: 0.6rem;
            margin-bottom: 0.5rem;
            background: #f8fafc;
            overflow: hidden;
        }

        .pro-theme-container .stamp-area img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
        }

        .pro-theme-container .signature-label {
            font-size: 0.7rem;
            color: #64748b;
            font-weight: 500;
        }

        .pro-theme-container .terms-section {
            margin: 1rem 0;
        }

        .pro-theme-container .terms-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.8rem;
        }

        .pro-theme-container .term-card {
            background: #f8fafc;
            border-radius: 6px;
            padding: 0.8rem;
            border: 1px solid #e2e8f0;
        }

        .pro-theme-container .term-card h3 {
            font-size: 0.7rem;
            font-weight: 600;
            color: #475569;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .pro-theme-container .term-content {
            font-size: 0.7rem;
            line-height: 1.3;
            color: #475569;
        }

        .pro-theme-container .pdf-footer {
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #e2e8f0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            font-size: 0.7rem;
            color: #64748b;
        }

        .pro-theme-container .footer-contact div {
            margin-bottom: 0.125rem;
        }

        .pro-theme-container .footer-thanks {
            text-align: right;
        }

        .pro-theme-container .footer-thanks .thanks-text {
            font-weight: 500;
            color: #1e293b;
            margin-bottom: 0.25rem;
        }
    `, [color]);

    const modernStyles = React.useMemo(() => `
        .modern-theme-container {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            line-height: 1.4;
            color: #1e293b;
            background: white;
            font-size: ${config.fontSize || 12}px;
            position: relative;
            overflow: hidden;
            border-radius: ${config.borderRadius || 6}px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .modern-theme-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, ${color}, #ededed);
        }

        .modern-theme-container, .modern-theme-container * {
            box-sizing: border-box;
        }
        
        .modern-theme-container .pdf-header {
            display: flex;
            justify-content: space-between;
            align-items: ${config.logoPosition === 'center' ? 'center' : 'flex-start'};
            flex-direction: ${config.logoPosition === 'center' ? 'column' : 'row'};
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #e2e8f0;
            text-align: ${config.logoPosition === 'center' ? 'center' : 'left'};
        }
        
        .modern-theme-container .header-left {
            display: flex;
            align-items: ${config.logoPosition === 'center' ? 'center' : 'flex-start'};
            flex-direction: ${config.logoPosition === 'center' ? 'column' : config.logoPosition === 'right' ? 'row-reverse' : 'row'};
            gap: 1.5rem;
            flex: 1;
            width: ${config.logoPosition === 'center' ? '100%' : 'auto'};
            text-align: ${config.logoPosition === 'right' ? 'right' : config.logoPosition === 'center' ? 'center' : 'left'};
        }
        
        .modern-theme-container .company-logo {
            width: 150px;
            height: 75px;
            border: 1px solid #e2e8f0;
            border-radius: ${config.borderRadius || 6}px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f8fafc;
            color: ${color};
            font-size: 0.6rem;
            text-align: center;
            padding: 0.5rem;
            overflow: hidden;
        }
        
        .modern-theme-container .company-logo img {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
        }
        
        .modern-theme-container .company-info {
            flex: 1;
        }
        
        .modern-theme-container .company-name {
            font-size: 1rem;
            font-weight: 700;
            color: ${color};
            margin-bottom: 0.25rem;
        }
        
        .modern-theme-container .company-details {
            font-size: 0.7rem;
            color: #000000;
            line-height: 1.3;
        }

        .modern-theme-container .header-info-grid {
            display: grid;
            gap: 0.125rem;
        }

        .modern-theme-container .header-info-line {
            display: flex;
        }

        .modern-theme-container .header-info-label {
            font-weight: 600;
            min-width: 60px;
            color: #475569;
        }
        
        .modern-theme-container .quote-info {
            text-align: ${config.logoPosition === 'right' ? 'left' : config.logoPosition === 'center' ? 'center' : 'right'};
            background: #f8fafc;
            padding: 0.8rem;
            border-radius: ${config.borderRadius || 6}px;
            border-left: ${config.logoPosition === 'right' ? 'none' : `3px solid ${color}`};
            border-right: ${config.logoPosition === 'right' ? `3px solid ${color}` : 'none'};
            border-top: ${config.logoPosition === 'center' ? `3px solid ${color}` : 'none'};
            min-width: 200px;
            margin-top: ${config.logoPosition === 'center' ? '1rem' : '0'};
            width: ${config.logoPosition === 'center' ? '100%' : 'auto'};
        }
        
        .modern-theme-container .quote-title {
            font-size: 0.9rem;
            font-weight: 700;
            color: ${color};
            margin-bottom: 0.5rem;
        }
        
        .modern-theme-container .quote-meta {
            font-size: 0.7rem;
            color: #64748b;
        }
        
        .modern-theme-container .quote-meta div {
            margin-bottom: 0.125rem;
        }
        
        .modern-theme-container .quote-number {
            font-weight: 600;
            color: #000000;
        }
        
        .modern-theme-container .customer-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1rem;
        }
        
        .modern-theme-container .customer-box {
            background: #f8fafc;
            border-radius: ${config.borderRadius || 6}px;
            padding: 0.8rem;
            border: 1px solid #e2e8f0;
        }
        
        .modern-theme-container .section-title {
            font-size: 0.8rem;
            font-weight: 600;
            color: ${color};
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.375rem;
        }
        
        .modern-theme-container .section-title i {
            color: ${color};
            font-size: 0.7rem;
        }
        
        .modern-theme-container .info-grid {
            display: grid;
            gap: 0.3rem;
        }
        
        .modern-theme-container .info-line {
            display: flex;
            font-size: 0.7rem;
        }
        
        .modern-theme-container .info-label {
            font-weight: 500;
            color: #475569;
            min-width: 60px;
        }
        
        .modern-theme-container .info-value {
            color: #1e293b;
            flex: 1;
        }
        
        .modern-theme-container .pdf-items-table {
            width: 100%;
            border-collapse: collapse;
            margin: 1rem 0;
            font-size: 0.7rem;
            background: white;
            border-radius: ${config.borderRadius || 6}px;
            letter-spacing: 0.05em;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .modern-theme-container .pdf-items-table th {
            background: ${config.tableHeaderBg || '#f1f5f9'};
            padding: 4px !important;
            height: auto !important;
            min-height: ${config.tableRowHeight || 35}px;
            vertical-align: middle;
            text-align: center;
            font-weight: 700;
            color: ${color};
            font-size: ${config.tableHeaderFontSize || 14}px !important;
            line-height: 1.2;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border-bottom: 1px solid #e2e8f0;
        }

        .pdf-items-table tr {
            page-break-inside: avoid;
            page-break-after: auto;
        }

        .pdf-items-table td img {
            max-width: 100%;
            height: auto;
            display: block;
        }
        
        .modern-theme-container .pdf-items-table td {
            padding: 0.4rem 0.3rem;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: middle;
        }
        
        .modern-theme-container .pdf-items-table tr:last-child td {
            border-bottom: none;
        }
        
        .modern-theme-container .pdf-items-table tr:nth-child(even) {
            background: #f8fafc;
        }

        .modern-theme-container .pdf-items-table tr:hover {
            background: #f1f5f9;
        }
        
        .modern-theme-container .item-image {
            width: 40px;
            height: 40px;
            border-radius: ${Math.max((config.borderRadius || 6) - 2, 2)}px;
            object-fit: cover;
            background: #f1f5f9;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #94a3b8;
            font-size: 0.5rem;
            text-align: center;
            overflow: hidden;
        }
        
        .modern-theme-container .item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 4px;
        }

        .modern-theme-container .item-description {
            font-weight: 500;
            color: #1e293b;
        }
        
        .modern-theme-container .item-details {
            font-size: 0.6rem;
            color: #64748b;
            margin-top: 0.2rem;
            line-height: 1.2;
        }
        
        .modern-theme-container .item-price, 
        .modern-theme-container .item-total {
            font-weight: 500;
            color: #1e293b;
            text-align: center;
        }
        
        .modern-theme-container .item-quantity, 
        .modern-theme-container .item-tax, 
        .modern-theme-container .item-unit {
            color: #64748b;
            text-align: center;
        }
        
        .modern-theme-container .pdf-summary {
            margin: 1rem 0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }
        
        .modern-theme-container .summary-box {
            background: white;
            border-radius: ${config.borderRadius || 6}px;
            padding: 0.8rem;
            border: 1px solid #e2e8f0;
        }
        
        .modern-theme-container .summary-row {
            display: flex;
            justify-content: space-between;
            padding: 0.3rem 0;
            border-bottom: 1px solid #f1f5f9;
            font-size: 0.7rem;
        }
        
        .modern-theme-container .summary-row:last-child {
            border-bottom: none;
        }
        
        .modern-theme-container .summary-row.grand-total {
            font-weight: 700;
            font-size: 0.8rem;
            color: ${color};
            padding-top: 0.5rem;
            margin-top: 0.25rem;
            border-top: 2px solid #e2e8f0;
        }
        
        .modern-theme-container .summary-title {
            font-size: 0.8rem;
            font-weight: 600;
            color: ${color};
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .modern-theme-container .payment-info h3 {
            font-size: 0.8rem;
            font-weight: 600;
            color: ${color};
            margin-bottom: 0.5rem;
        }
        
        .modern-theme-container .signature-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin: 1rem 0;
            padding-top: 1rem;
            border-top: 1px solid #e2e8f0;
        }
        
        .modern-theme-container .signature-box {
            text-align: center;
            padding: 0.8rem;
        }
        
        .modern-theme-container .signature-area {
            height: 60px;
            border-bottom: 1px solid #cbd5e1;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: flex-end;
            justify-content: center;
            position: relative;
        }
        
        .modern-theme-container .stamp-area {
            height: 60px;
            border: 1px dashed #cbd5e1;
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #94a3b8;
            font-size: 0.6rem;
            margin-bottom: 0.5rem;
            background: #f8fafc;
            overflow: hidden;
        }
        
        .modern-theme-container .signature-label {
            font-size: 0.7rem;
            color: #64748b;
            font-weight: 500;
        }
        
        .modern-theme-container .terms-section {
            margin: 1rem 0;
        }
        
        .modern-theme-container .terms-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
        .modern-theme-container .pdf-footer {
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #e2e8f0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            font-size: 0.7rem;
            color: #64748b;
        }
        
        .modern-theme-container .footer-contact div {
            margin-bottom: 0.125rem;
        }
        
        .modern-theme-container .footer-thanks {
            text-align: right;
        }
        
        .modern-theme-container .footer-thanks .thanks-text {
            font-weight: 500;
            color: ${color};
            margin-bottom: 0.25rem;
        }

        /* PDF Optimizasyon Stilleri */
        .pdf-compact-mode .pdf-items-table {
            font-size: 6px !important;
        }

        .pdf-compact-mode .pdf-items-table th {
            padding: 3px 2px !important;
            line-height: 1.1 !important;
            font-size: 8px !important;
            text-align: center !important;
            height: 35px !important;
            vertical-align: middle !important;
        }

        .pdf-compact-mode .pdf-items-table td {
            padding: 2px 1px !important;
            line-height: 1 !important;
            vertical-align: middle !important;
        }

        .pdf-compact-mode .item-price,
        .pdf-compact-mode .item-total,
        .pdf-compact-mode .item-quantity,
        .pdf-compact-mode .item-tax,
        .pdf-compact-mode .item-unit {
            text-align: center !important;
        }

        .pdf-compact-mode .item-image {
            width: 25px !important;
            height: 25px !important;
        }

        .pdf-compact-mode .section-title {
            font-size: 7px !important;
            margin-bottom: 3px !important;
        }

        .pdf-compact-mode body {
            font-size: 8px !important;
            line-height: 1.1 !important;
        }

        /* Ultra kompakt mod */
        .pdf-ultra-compact .pdf-items-table {
            font-size: 5px !important;
        }

        .pdf-ultra-compact .pdf-items-table td,
        .pdf-ultra-compact .pdf-items-table th {
            padding: 1px 0.5px !important;
            line-height: 0.9 !important;
        }

        .pdf-ultra-compact .item-image {
            width: 20px !important;
            height: 20px !important;
        }

        /* Sayfa sonu optimizasyonu */
        .page-break-avoid {
            page-break-inside: avoid;
        }

        .keep-together {
            page-break-inside: avoid;
        }

        /* PDF Sayfa Düzeni Optimizasyonları */
        .pdf-items-table-compact {
            font-size: 9px !important;
            line-height: 1.1 !important;
        }

        .pdf-items-table-compact th,
        .pdf-items-table-compact td {
            padding: 3px 2px !important;
            line-height: 1.1 !important;
        }

        .pdf-items-table-compact th {
            font-size: 8px !important;
            padding: 4px 2px !important;
        }

        .item-image-compact {
            width: 25px !important;
            height: 25px !important;
            min-width: 25px !important;
            min-height: 25px !important;
        }

        .item-image-compact img {
            width: 100% !important;
            height: 100% !important;
            object-fit: cover !important;
        }

        /* Tablo başlığını her sayfada tekrarla */
        .pdf-items-table thead {
            display: table-header-group;
        }

        .pdf-items-table tbody {
            display: table-row-group;
        }

        /* Özet bölümü için sayfa sonu optimizasyonu */
        .pdf-summary {
            page-break-inside: avoid;
        }

        /* İmza bölümü için sayfa sonu optimizasyonu */
        .signature-section {
            page-break-inside: avoid;
            margin-top: 20px;
        }
    `, [config, color]);



    const classicStyles = React.useMemo(() => `
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Merriweather:ital,wght@0,300;0,400;0,700;1,300&display=swap');

        .classic-theme-container {
            font-family: 'Merriweather', serif;
            color: #2c3e50;
            background: white;
            font-size: 10pt;
            line-height: 1.6;
            max-width: 210mm;
            margin: 0 auto;
            padding: 40px;
            position: relative;
        }

        .classic-theme-container .classic-border {
            border: 1px solid #000;
            padding: 4px;
            height: 100%;
        }

        .classic-theme-container .classic-inner-border {
            border: 3px double #000;
            padding: 40px;
            min-height: 250mm;
            position: relative;
        }

        .classic-theme-container .header {
            text-align: center;
            margin-bottom: 40px;
            position: relative;
        }

        .classic-theme-container .header::after {
            content: '';
            display: block;
            width: 100px;
            height: 2px;
            background: #000;
            margin: 20px auto 0;
        }

        .classic-theme-container .company-logo {
            margin-bottom: 20px;
        }

        .classic-theme-container .company-name {
            font-family: 'Playfair Display', serif;
            font-size: 28pt;
            font-weight: 700;
            letter-spacing: 1px;
            margin-bottom: 10px;
            color: #000;
        }

        .classic-theme-container .company-info {
            font-style: italic;
            color: #555;
            font-size: 9pt;
        }

        .classic-theme-container .quote-title {
            text-align: center;
            font-family: 'Playfair Display', serif;
            font-size: 20pt;
            font-weight: 700;
            margin: 30px 0;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .classic-theme-container .info-section {
            display: flex;
            justify-content: space-between;
            margin-bottom: 40px;
            border-top: 1px solid #eee;
            border-bottom: 1px solid #eee;
            padding: 20px 0;
        }

        .classic-theme-container .info-block h3 {
            font-family: 'Playfair Display', serif;
            font-size: 11pt;
            font-weight: 700;
            text-transform: uppercase;
            margin-bottom: 10px;
            color: #000;
            letter-spacing: 1px;
        }

        .classic-theme-container .info-value {
            margin-bottom: 4px;
        }

        .classic-theme-container table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 40px;
        }

        .classic-theme-container th {
            font-family: 'Playfair Display', serif;
            text-align: left;
            padding: 15px 10px;
            font-weight: 700;
            font-size: 10pt;
            text-transform: uppercase;
            border-bottom: 2px solid #000;
            letter-spacing: 1px;
        }

        .classic-theme-container td {
            padding: 15px 10px;
            border-bottom: 1px solid #e0e0e0;
        }

        .classic-theme-container .totals {
            width: 250px;
            margin-left: auto;
            margin-top: 20px;
        }

        .classic-theme-container .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 10pt;
        }

        .classic-theme-container .grand-total {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            font-size: 14pt;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            padding: 15px 0;
            margin-top: 15px;
        }

        .classic-theme-container .footer {
            text-align: center;
            margin-top: 60px;
            font-size: 8pt;
            color: #888;
            font-style: italic;
        }
    `, []);

    const minimalStyles = React.useMemo(() => `
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

        .minimal-theme-container {
            font-family: 'Inter', sans-serif;
            color: #111;
            background: white;
            font-size: 10pt;
            line-height: 1.5;
            max-width: 210mm;
            margin: 0 auto;
            padding: 50px;
        }

        .minimal-theme-container .header {
            margin-bottom: 80px;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        .minimal-theme-container .brand {
            font-size: 24pt;
            font-weight: 800;
            letter-spacing: -1px;
            line-height: 1;
        }

        .minimal-theme-container .quote-meta {
            text-align: right;
        }

        .minimal-theme-container .meta-item {
            margin-bottom: 5px;
            font-size: 9pt;
            color: #666;
        }

        .minimal-theme-container .meta-item strong {
            color: #000;
            font-weight: 600;
            margin-right: 5px;
        }

        .minimal-theme-container .addresses {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            margin-bottom: 80px;
        }

        .minimal-theme-container .address-block h3 {
            font-size: 8pt;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #999;
            margin-bottom: 15px;
        }

        .minimal-theme-container .address-block div {
            margin-bottom: 3px;
        }

        .minimal-theme-container .main-title {
            font-size: 32pt;
            font-weight: 300;
            letter-spacing: -1px;
            margin-bottom: 40px;
            color: #000;
        }

        .minimal-theme-container table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 60px;
        }

        .minimal-theme-container th {
            text-align: left;
            font-weight: 600;
            color: #999;
            font-size: 9pt;
            padding-bottom: 20px;
            border-bottom: 1px solid #eee;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .minimal-theme-container td {
            padding: 20px 0;
            border-bottom: 1px solid #f5f5f5;
            vertical-align: top;
        }

        .minimal-theme-container .item-name {
            font-weight: 600;
            font-size: 11pt;
            margin-bottom: 5px;
        }

        .minimal-theme-container .item-desc {
            font-size: 9pt;
            color: #666;
        }

        .minimal-theme-container .totals {
            width: 300px;
            margin-left: auto;
        }

        .minimal-theme-container .total-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            font-size: 10pt;
            color: #444;
        }

        .minimal-theme-container .grand-total {
            font-weight: 700;
            font-size: 18pt;
            color: #000;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 2px solid #000;
            align-items: center;
        }

        .minimal-theme-container .footer {
            margin-top: 100px;
            padding-top: 20px;
            border-top: 1px solid #f5f5f5;
            display: flex;
            justify-content: space-between;
            font-size: 9pt;
            color: #999;
        }
    `, []);

    const corporateStyles = React.useMemo(() => `
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap');

        .corporate-theme-container {
            font-family: 'Roboto', sans-serif;
            color: #333;
            background: white;
            font-size: 10pt;
            line-height: 1.5;
            max-width: 210mm;
            margin: 0 auto;
            padding: 0;
        }

        .corporate-theme-container .header {
            background: ${color};
            color: white;
            padding: 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .corporate-theme-container .company-name {
            font-size: 24pt;
            font-weight: 700;
            margin-bottom: 5px;
        }

        .corporate-theme-container .company-details {
            font-size: 9pt;
            opacity: 0.9;
        }

        .corporate-theme-container .quote-badge {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px 25px;
            border-radius: 8px;
            text-align: right;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .corporate-theme-container .quote-badge .label {
            font-size: 8pt;
            text-transform: uppercase;
            letter-spacing: 1px;
            opacity: 0.8;
            margin-bottom: 5px;
        }

        .corporate-theme-container .quote-badge .value {
            font-size: 16pt;
            font-weight: 700;
        }

        .corporate-theme-container .content {
            padding: 40px;
        }

        .corporate-theme-container .grid-box {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }

        .corporate-theme-container .box-title {
            font-size: 9pt;
            font-weight: 700;
            text-transform: uppercase;
            color: ${color};
            border-bottom: 2px solid ${color};
            padding-bottom: 10px;
            margin-bottom: 15px;
            letter-spacing: 0.5px;
        }

        .corporate-theme-container .box-content {
            font-size: 10pt;
        }

        .corporate-theme-container table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 40px;
            border: 1px solid #e2e8f0;
        }

        .corporate-theme-container th {
            background: #f8fafc;
            color: #475569;
            text-align: left;
            padding: 15px;
            font-weight: 700;
            font-size: 9pt;
            text-transform: uppercase;
            border-bottom: 2px solid #e2e8f0;
        }

        .corporate-theme-container td {
            padding: 15px;
            border-bottom: 1px solid #e2e8f0;
            vertical-align: middle;
        }

        .corporate-theme-container tr:nth-child(even) {
            background: #fcfcfc;
        }

        .corporate-theme-container .totals {
            background: #f8fafc;
            padding: 25px;
            border-radius: 8px;
            width: 350px;
            margin-left: auto;
            border: 1px solid #e2e8f0;
        }

        .corporate-theme-container .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 10pt;
            color: #475569;
        }

        .corporate-theme-container .grand-total {
            border-top: 2px solid ${color};
            margin-top: 15px;
            padding-top: 15px;
            font-weight: 700;
            font-size: 16pt;
            color: ${color};
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .corporate-theme-container .footer {
            margin-top: 60px;
            text-align: center;
            font-size: 9pt;
            color: #64748b;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
        }
    `, [color]);



    const getContainerStyles = () => {
        const baseStyles = {
            minHeight: 'auto',
            fontFamily: config.fontFamily === 'Playfair Display' ? "'Playfair Display', serif" :
                config.fontFamily === 'Roboto' ? "'Roboto', sans-serif" :
                    "'Inter', sans-serif",
            fontSize: `${config.fontSize || 12} px`
        };

        // Margins (Padding)
        const marginMap = {
            compact: '1.5rem', // ~6mm
            normal: '1.5rem',  // ~10mm (Adjusted to match preview.html which uses 1.5rem)
            wide: '4rem'       // ~16mm
        };
        const padding = marginMap[config.margins] || '1.5rem';

        return { ...baseStyles, padding };
    };

    const containerStyles = getContainerStyles();







    // Default layout if not provided
    const defaultLayout = [
        { id: 'header', enabled: true },
        { id: 'customer', enabled: true },
        { id: 'table', enabled: true },
        { id: 'notes', enabled: true },
        { id: 'signatures', enabled: true },
        { id: 'footer', enabled: true }
    ];

    const activeLayout = layout || defaultLayout;

    const renderSection = (sectionId) => {
        switch (sectionId) {
            case 'header':
                return (
                    <div className="pdf-header">
                        <div className="header-left">
                            {config.showLogo && (
                                <div className="company-logo">
                                    {companyData.logo ? (
                                        <img src={companyData.logo} alt="Logo" />
                                    ) : (
                                        <div>LOGO</div>
                                    )}
                                </div>
                            )}
                            <div className="company-info">
                                <div className="company-name">{companyData.name}</div>
                                <div className="company-details header-info-grid">
                                    <div className="header-info-line">
                                        <span className="header-info-label">{t.phone}:</span>
                                        <span>{companyData.phone}</span>
                                    </div>
                                    <div className="header-info-line">
                                        <span className="header-info-label">{t.address}:</span>
                                        <span>{companyData.address}</span>
                                    </div>
                                    <div className="header-info-line">
                                        <span className="header-info-label">{t.email}:</span>
                                        <span>{companyData.email}</span>
                                    </div>
                                    <div className="header-info-line">
                                        <span className="header-info-label">Web:</span>
                                        <span>{companyData.website || '-'}</span>
                                    </div>
                                    <div className="header-info-line">
                                        <span className="header-info-label">{t.authorized}:</span>
                                        <span>{companyData.authorized}</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div className="quote-info" style={{ borderColor: color }}>
                            <div className="quote-title">{config.title}</div>
                            <div className="quote-meta">
                                <div><strong>{t.quoteNo}:</strong> <span className="quote-number">{quoteData.number}</span></div>
                                <div><strong>{t.date}:</strong> {formatDate(quoteData.date, currentLocale)}</div>
                                <div><strong>{t.validUntil}:</strong> {formatDate(quoteData.validUntil, currentLocale)}</div>
                                <div><strong>{t.payment}:</strong> {quoteData.terms || '-'}</div>
                            </div>
                        </div>
                    </div>
                );

            case 'customer':
                return (
                    <div className="customer-section">
                        <div className="customer-box">
                            <div className="section-title">
                                <i className="fas fa-building"></i> SATICI
                            </div>
                            <div className="info-grid">
                                <div className="info-line">
                                    <span className="info-label">Firma:</span>
                                    <span className="info-value">{companyData.name}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">Yetkili:</span>
                                    <span className="info-value">{companyData.authorized}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">Tel:</span>
                                    <span className="info-value">{companyData.phone}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">E-posta:</span>
                                    <span className="info-value">{companyData.email}</span>
                                </div>
                            </div>
                        </div>

                        <div className="customer-box">
                            <div className="section-title">
                                <i className="fas fa-user"></i> {t.customer}
                            </div>
                            <div className="info-grid">
                                <div className="info-line">
                                    <span className="info-label">{t.company}:</span>
                                    <span className="info-value">{customerData.company}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.authorized}:</span>
                                    <span className="info-value">{customerData.name}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.phone}:</span>
                                    <span className="info-value">{customerData.phone}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.email}:</span>
                                    <span className="info-value">{customerData.email}</span>
                                </div>
                                {customerData.taxOffice && (
                                    <div className="info-line">
                                        <span className="info-label">{t.tax}:</span>
                                        <span className="info-value">{customerData.taxOffice} / {customerData.taxNo}</span>
                                    </div>
                                )}
                            </div>
                        </div>
                    </div>
                );

            case 'table':
                return (
                    <>
                        <table className="pdf-items-table">
                            <thead>
                                <tr>
                                    <th width="25" style={{ textAlign: 'center' }}>#</th>
                                    {config.showTableImages && <th width="45" style={{ textAlign: 'center' }}>{t.image}</th>}
                                    <th style={{ textAlign: 'left' }}>{t.item} / {t.description}</th>
                                    {config.showTableUnit && <th width="60" style={{ textAlign: 'center' }}>{t.unit}</th>}
                                    <th width="60" style={{ textAlign: 'center' }}>{t.quantity}</th>
                                    <th width="90" style={{ textAlign: 'center' }}>{t.unitPrice}</th>
                                    {hasLineItemDiscounts && <th width="60" style={{ textAlign: 'center' }}>{t.discount}</th>}
                                    {config.showTableTax && <th width="50" style={{ textAlign: 'center' }}>{t.vat}</th>}
                                    <th width="90" style={{ textAlign: 'center' }}>{t.total}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {items.map((item, index) => (
                                    <tr key={index}>
                                        <td style={{ textAlign: 'center' }}>{index + 1}</td>
                                        {config.showTableImages && (
                                            <td style={{ textAlign: 'center' }}>
                                                <div className="item-image">
                                                    {item.image ? (
                                                        <img src={item.image} alt="" />
                                                    ) : (
                                                        '-'
                                                    )}
                                                </div>
                                            </td>
                                        )}
                                        <td>
                                            <div className="item-description">{item.name}</div>
                                            {item.description && (
                                                <div className="item-details">
                                                    {item.description}
                                                </div>
                                            )}
                                        </td>
                                        {config.showTableUnit && <td className="item-unit">{item.unit}</td>}
                                        <td className="item-quantity">{item.quantity}</td>
                                        <td className="item-price">{formatCurrency(item.price)}</td>
                                        {hasLineItemDiscounts && <td className="item-discount" style={{ textAlign: 'center', color: '#ef4444' }}>%{item.discountRate || 0}</td>}
                                        {config.showTableTax && <td className="item-tax">%{item.taxRate}</td>}
                                        <td className="item-total">
                                            {formatCurrency((item.quantity || 0) * (item.price || 0) * (1 - (item.discountRate || 0) / 100))}
                                        </td>
                                    </tr>
                                ))}
                            </tbody>
                        </table>

                        <div className="pdf-summary">
                            {config.showSummary && (
                                <div className="summary-box">
                                    <div className="summary-title">
                                        <i className="fas fa-calculator"></i> {t.summary}
                                    </div>
                                    <div className="summary-row">
                                        <span className="text-slate-500">{t.subtotal}:</span>
                                        <span className="font-medium text-slate-900">{formatCurrency(subtotal)}</span>
                                    </div>
                                    {discountAmount > 0 && (
                                        <div className="summary-row">
                                            <span className="text-red-500">{t.discount} {discount?.type === 'percentage' ? `(% ${discount.value})` : ''}:</span>
                                            <span className="font-medium text-red-500">-{formatCurrency(discountAmount)}</span>
                                        </div>
                                    )}
                                    {config.showTableTax && (
                                        <>
                                            {Object.entries(taxBreakdown).map(([rate, amount]) => (
                                                <div key={rate} className="summary-row">
                                                    <span className="text-slate-500">{t.vat} (%{rate}):</span>
                                                    <span className="font-medium text-slate-900">{formatCurrency(amount)}</span>
                                                </div>
                                            ))}
                                            {Object.keys(taxBreakdown).length === 0 && totalTax > 0 && (
                                                <div className="summary-row">
                                                    <span className="text-slate-500">{t.vat}:</span>
                                                    <span className="font-medium text-slate-900">{formatCurrency(totalTax)}</span>
                                                </div>
                                            )}
                                            <div className="summary-row">
                                                <span className="font-semibold text-slate-700">{t.total} {t.vat}:</span>
                                                <span className="font-semibold text-slate-900">{formatCurrency(totalTax)}</span>
                                            </div>
                                        </>
                                    )}
                                    <div className="summary-row grand-total">
                                        <span>{t.generalTotal}:</span>
                                        <span>{formatCurrency(total)}</span>
                                    </div>
                                </div>
                            )}

                            <div>
                                {config.showBankInfo && (
                                    <div className="summary-box" style={{ marginBottom: config.showNotes ? '1rem' : 0 }}>
                                        <div className="summary-title">
                                            {t.bankInfo}
                                        </div>
                                        <div className="info-grid">
                                            <div className="info-line">
                                                <span className="info-label">{t.bank}:</span>
                                                <span className="info-value">{bankData.bankName || '-'}</span>
                                            </div>
                                            <div className="info-line">
                                                <span className="info-label">{t.branch}:</span>
                                                <span className="info-value">{bankData.branch || '-'}</span>
                                            </div>
                                            <div className="info-line">
                                                <span className="info-label">{t.accountNo}:</span>
                                                <span className="info-value">{bankData.accountNo || '-'}</span>
                                            </div>
                                            <div className="info-line">
                                                <span className="info-label">{t.iban}:</span>
                                                <span className="info-value" style={{ fontFamily: 'monospace' }}>{bankData.iban || '-'}</span>
                                            </div>
                                            <div className="info-line">
                                                <span className="info-label">{t.accountHolder}:</span>
                                                <span className="info-value">{bankData.accountHolder || '-'}</span>
                                            </div>
                                        </div>
                                    </div>
                                )}
                            </div>
                        </div>
                    </>
                );

            case 'notes':
                return (
                    <>
                        {config.showNotes && quoteData.notes && (
                            <div className="summary-box" style={{ marginTop: '1rem', marginBottom: '1rem' }}>
                                <div className="summary-title">{t.notes}</div>
                                <div style={{ color: '#475569', lineHeight: 1.3, fontSize: '0.7rem' }}>{quoteData.notes}</div>
                            </div>
                        )}
                        {config.showTerms && (
                            <div className="terms-section">
                                <div className="terms-grid">
                                    {(quoteData.validUntil || quoteData.terms) && (
                                        <div className="term-card">
                                            <h3>{t.terms}</h3>
                                            <div className="term-content">
                                                {quoteData.validUntil && (
                                                    <div className="mb-1">
                                                        <strong>{t.validUntil}:</strong> {formatDate(quoteData.validUntil, currentLocale)} {t.validUntilDate}.
                                                    </div>
                                                )}
                                                {quoteData.terms && (
                                                    <div>
                                                        <strong>{t.payment}:</strong> {quoteData.terms}
                                                    </div>
                                                )}
                                            </div>
                                        </div>
                                    )}
                                    {(quoteData.deliveryTerms || quoteData.warrantyTerms) && (
                                        <div className="term-card">
                                            <h3>{t.deliveryWarranty}</h3>
                                            <div className="term-content">
                                                {quoteData.deliveryTerms && (
                                                    <div className="mb-1">
                                                        <strong>{t.delivery}:</strong> {quoteData.deliveryTerms}
                                                    </div>
                                                )}
                                                {quoteData.warrantyTerms && (
                                                    <div>
                                                        <strong>{t.warranty}:</strong> {quoteData.warrantyTerms}
                                                    </div>
                                                )}
                                            </div>
                                        </div>
                                    )}
                                </div>
                            </div>
                        )}
                    </>
                );

            case 'signatures':
                return config.showSignatures ? (
                    <div className="signature-section">
                        <div className="signature-box">
                            <div className="signature-area">
                                {signature ? (
                                    <img src={signature} alt="Dijital İmza" style={{ maxHeight: '100%', maxWidth: '100%', objectFit: 'contain' }} />
                                ) : companyData.signature ? (
                                    <img src={companyData.signature} alt="İmza" style={{ maxHeight: '100%', maxWidth: '100%', objectFit: 'contain' }} />
                                ) : (
                                    <span style={{ color: '#cbd5e1', fontSize: '0.7rem' }}>{t.signature}</span>
                                )}
                            </div>
                            <div className="signature-label">{t.deliveredBy}</div>
                        </div>
                        <div className="signature-box">
                            <div className="stamp-area" style={companyData.stamp ? { border: 'none', background: 'transparent' } : {}}>
                                {companyData.stamp ? (
                                    <img src={companyData.stamp} alt="Kaşe" style={{ maxHeight: '100%', maxWidth: '100%', objectFit: 'contain' }} />
                                ) : (
                                    <span style={{ color: '#cbd5e1', fontSize: '0.7rem' }}>{t.stamp} / {t.signature}</span>
                                )}
                            </div>
                            <div className="signature-label">{t.receivedBy}</div>
                        </div>
                    </div>
                ) : null;

            case 'footer':
                return (
                    <>
                        <div className="pdf-footer">
                            <div className="footer-contact">
                                <div>{companyData.address}</div>
                                <div>{companyData.phone} | {companyData.email}</div>
                                <div>{companyData.website}</div>
                            </div>
                            <div className="footer-thanks">
                            </div>
                        </div>
                        {config.customFooter && (
                            <div style={{
                                marginTop: '1rem',
                                paddingTop: '0.5rem',
                                borderTop: '1px solid #e2e8f0',
                                textAlign: 'center',
                                fontSize: '0.65rem',
                                color: '#94a3b8'
                            }}>
                                {config.customFooter}
                            </div>
                        )}
                    </>
                );

            default:
                return null;
        }
    };

    // Modern Theme Render
    if (theme === 'modern') {
        return (
            <>
                <div id={id} className={`modern - theme - container w - full max - w - [210mm] mx - auto ${config.tableDensity === 'compact' ? 'pdf-compact-mode' :
                    config.tableDensity === 'ultra-compact' ? 'pdf-ultra-compact' : ''
                    } `} style={containerStyles}>
                    <style>{modernStyles}</style>

                    {/* Watermark */}
                    {config.showWatermark && (
                        <div
                            style={{
                                position: 'absolute',
                                inset: 0,
                                display: 'flex',
                                alignItems: 'center',
                                justifyContent: 'center',
                                pointerEvents: 'none',
                                zIndex: 0,
                                transform: `rotate(${config.watermarkRotation || -45}deg)`,
                                opacity: config.watermarkOpacity,
                                fontSize: `${config.watermarkFontSize || 120} px`,
                                fontWeight: 'bold',
                                color: config.watermarkColor || '#000000',
                                whiteSpace: 'nowrap'
                            }}
                        >
                            {config.watermarkText}
                        </div>
                    )}

                    {activeLayout.map(section => {
                        if (!section.enabled) return null;
                        return (
                            <React.Fragment key={section.id}>
                                {renderSection(section.id)}
                            </React.Fragment>
                        );
                    })}
                </div>
            </>
        );
    }

    // Classic Theme Render
    if (theme === 'classic') {
        return (
            <div id={id} className="classic-theme-container" style={containerStyles}>
                <style>{classicStyles}</style>
                <div className="classic-border">
                    <div className="classic-inner-border">
                        <div className="header">
                            {config.showLogo && companyData.logo && (
                                <div className="company-logo">
                                    <img src={companyData.logo} alt="Logo" style={{ height: '80px', objectFit: 'contain' }} />
                                </div>
                            )}
                            <div className="company-name">{companyData.name}</div>
                            <div className="company-info">
                                <div>{companyData.address}</div>
                                <div>{companyData.phone} | {companyData.email}</div>
                                <div>{companyData.website}</div>
                            </div>
                        </div>

                        <div className="quote-title">
                            {config.title}
                        </div>

                        <div className="info-section">
                            <div className="info-block">
                                <h3>{t.customer}</h3>
                                <div className="info-value"><strong>{customerData.company}</strong></div>
                                <div className="info-value">{customerData.name}</div>
                                <div className="info-value">{customerData.phone}</div>
                                <div className="info-value">{customerData.email}</div>
                            </div>
                            <div className="info-block" style={{ textAlign: 'right' }}>
                                <h3>{t.details}</h3>
                                <div className="info-value"><strong>{t.quoteNo}:</strong> #{quoteData.number}</div>
                                <div className="info-value"><strong>{t.date}:</strong> {formatDate(quoteData.date, currentLocale)}</div>
                                <div className="info-value"><strong>{t.validUntil}:</strong> {formatDate(quoteData.validUntil, currentLocale)}</div>
                            </div>
                        </div>

                        <table className="pdf-items-table">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    {config.showTableImages && <th>{t.image}</th>}
                                    <th>{t.item}</th>
                                    <th>{t.quantity}</th>
                                    {config.showTableUnit && <th>{t.unit}</th>}
                                    <th>{t.unitPrice}</th>
                                    {config.showTableTax && <th>{t.tax}</th>}
                                    <th>{t.total}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {items.map((item, index) => (
                                    <tr key={index}>
                                        <td>{index + 1}</td>
                                        {config.showTableImages && (
                                            <td>
                                                {item.image && <img src={item.image} alt="" style={{ width: '30px', height: '30px', objectFit: 'cover' }} />}
                                            </td>
                                        )}
                                        <td>
                                            <div style={{ fontWeight: '700', fontSize: '11pt' }}>{item.name}</div>
                                            <div style={{ fontSize: '9pt', color: '#666', fontStyle: 'italic' }}>{item.description}</div>
                                        </td>
                                        <td>{item.quantity}</td>
                                        {config.showTableUnit && <td>{item.unit}</td>}
                                        <td>{formatCurrency(item.price)}</td>
                                        {config.showTableTax && <td>%{item.taxRate}</td>}
                                        <td>{formatCurrency(item.total)}</td>
                                    </tr>
                                ))}
                            </tbody>
                        </table>

                        <div className="totals">
                            <div className="total-row">
                                <span>{t.subtotal}:</span>
                                <span>{formatCurrency(subtotal)}</span>
                            </div>
                            {discount > 0 && (
                                <div className="total-row">
                                    <span>{t.discount}:</span>
                                    <span>-{formatCurrency(discountAmount)}</span>
                                </div>
                            )}
                            <div className="total-row">
                                <span>{t.tax}:</span>
                                <span>{formatCurrency(totalTax)}</span>
                            </div>
                            <div className="grand-total" style={{ display: 'flex', justifyContent: 'space-between' }}>
                                <span>{t.generalTotal}:</span>
                                <span>{formatCurrency(total)}</span>
                            </div>
                        </div>

                        {config.showSignatures && (
                            <div style={{ marginTop: '80px', display: 'flex', justifyContent: 'space-between', fontFamily: 'Merriweather, serif' }}>
                                <div style={{ textAlign: 'center' }}>
                                    <div style={{ borderBottom: '1px solid #000', paddingBottom: '5px', marginBottom: '10px', width: '200px', fontStyle: 'italic' }}>{t.deliveredBy}</div>
                                    {signature && <img src={signature} alt="" style={{ height: '50px' }} />}
                                </div>
                                <div style={{ textAlign: 'center' }}>
                                    <div style={{ borderBottom: '1px solid #000', paddingBottom: '5px', marginBottom: '10px', width: '200px', fontStyle: 'italic' }}>{t.receivedBy}</div>
                                    {companyData.stamp && <img src={companyData.stamp} alt="" style={{ height: '50px' }} />}
                                </div>
                            </div>
                        )}

                        <div className="footer">
                            {companyData.name} - {companyData.address} | {companyData.website}
                        </div>
                    </div>
                </div>
            </div>
        );
    }

    // Minimal Theme Render
    if (theme === 'minimal') {
        return (
            <div id={id} className="minimal-theme-container" style={containerStyles}>
                <style>{minimalStyles}</style>

                <div className="header">
                    <div className="brand">{companyData.name}</div>
                    <div className="quote-meta">
                        <div className="meta-item"><strong>{t.quoteNo}:</strong> #{quoteData.number}</div>
                        <div className="meta-item"><strong>{t.date}:</strong> {formatDate(quoteData.date, currentLocale)}</div>
                        <div className="meta-item"><strong>{t.validUntil}:</strong> {formatDate(quoteData.validUntil, currentLocale)}</div>
                    </div>
                </div>

                <div className="addresses">
                    <div className="address-block">
                        <h3>{t.from}</h3>
                        <div style={{ fontWeight: '600' }}>{companyData.name}</div>
                        <div>{companyData.address}</div>
                        <div>{companyData.email}</div>
                        <div>{companyData.phone}</div>
                    </div>
                    <div className="address-block">
                        <h3>{t.to}</h3>
                        <div style={{ fontWeight: '600' }}>{customerData.company}</div>
                        <div>{customerData.name}</div>
                        <div>{customerData.email}</div>
                        <div>{customerData.phone}</div>
                    </div>
                </div>

                <div className="main-title">
                    {config.title}
                </div>

                <table className="pdf-items-table">
                    <thead>
                        <tr>
                            <th>{t.item}</th>
                            <th style={{ textAlign: 'center' }}>{t.quantity}</th>
                            <th style={{ textAlign: 'right' }}>{t.unitPrice}</th>
                            <th style={{ textAlign: 'right' }}>{t.total}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {items.map((item, index) => (
                            <tr key={index}>
                                <td>
                                    <div className="item-name">{item.name}</div>
                                    <div className="item-desc">{item.description}</div>
                                </td>
                                <td style={{ textAlign: 'center' }}>{item.quantity} {item.unit}</td>
                                <td style={{ textAlign: 'right' }}>{formatCurrency(item.price)}</td>
                                <td style={{ textAlign: 'right' }}>{formatCurrency(item.total)}</td>
                            </tr>
                        ))}
                    </tbody>
                </table>

                <div className="totals">
                    <div className="total-row">
                        <span>{t.subtotal}</span>
                        <span>{formatCurrency(subtotal)}</span>
                    </div>
                    {discount > 0 && (
                        <div className="total-row">
                            <span>{t.discount}</span>
                            <span>-{formatCurrency(discountAmount)}</span>
                        </div>
                    )}
                    <div className="total-row">
                        <span>{t.tax}</span>
                        <span>{formatCurrency(totalTax)}</span>
                    </div>
                    <div className="grand-total" style={{ display: 'flex', justifyContent: 'space-between' }}>
                        <span>{t.generalTotal}</span>
                        <span>{formatCurrency(total)}</span>
                    </div>
                </div>

                <div className="footer">
                    <div>{companyData.website}</div>
                    <div>{companyData.address}</div>
                </div>
            </div>
        );
    }

    // Corporate Theme Render
    if (theme === 'corporate') {
        return (
            <div id={id} className="corporate-theme-container" style={containerStyles}>
                <style>{corporateStyles}</style>

                <div className="header-bg"></div>

                <div className="header">
                    <div>
                        <div className="company-name">{companyData.name}</div>
                        <div className="company-details">
                            <div>{companyData.address}</div>
                            <div>{companyData.email}</div>
                        </div>
                    </div>
                    <div className="quote-badge">
                        <div className="label">{t.quoteNo}</div>
                        <div className="value">#{quoteData.number}</div>
                    </div>
                </div>

                <div className="content">
                    <div className="grid-box">
                        <div className="box">
                            <div className="box-title">{t.customer}</div>
                            <div className="box-content">
                                <div style={{ fontWeight: '700', marginBottom: '5px', fontSize: '11pt' }}>{customerData.company}</div>
                                <div>{customerData.name}</div>
                                <div>{customerData.phone}</div>
                                <div>{customerData.email}</div>
                            </div>
                        </div>
                        <div className="box">
                            <div className="box-title">{t.details}</div>
                            <div className="box-content">
                                <div className="total-row">
                                    <span style={{ fontWeight: '500' }}>{t.date}:</span>
                                    <span>{formatDate(quoteData.date, currentLocale)}</span>
                                </div>
                                <div className="total-row">
                                    <span style={{ fontWeight: '500' }}>{t.validUntil}:</span>
                                    <span>{formatDate(quoteData.validUntil, currentLocale)}</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <table className="pdf-items-table">
                        <thead>
                            <tr>
                                <th>{t.item}</th>
                                <th style={{ textAlign: 'center' }}>{t.quantity}</th>
                                <th style={{ textAlign: 'right' }}>{t.unitPrice}</th>
                                <th style={{ textAlign: 'right' }}>{t.total}</th>
                            </tr>
                        </thead>
                        <tbody>
                            {items.map((item, index) => (
                                <tr key={index}>
                                    <td>
                                        <div style={{ fontWeight: '700' }}>{item.name}</div>
                                        <div style={{ fontSize: '9pt', color: '#64748b' }}>{item.description}</div>
                                    </td>
                                    <td style={{ textAlign: 'center' }}>{item.quantity} {item.unit}</td>
                                    <td style={{ textAlign: 'right' }}>{formatCurrency(item.price)}</td>
                                    <td style={{ textAlign: 'right' }}>{formatCurrency(item.total)}</td>
                                </tr>
                            ))}
                        </tbody>
                    </table>

                    <div className="totals">
                        <div className="total-row">
                            <span>{t.subtotal}</span>
                            <span>{formatCurrency(subtotal)}</span>
                        </div>
                        {discount > 0 && (
                            <div className="total-row">
                                <span>{t.discount}</span>
                                <span>-{formatCurrency(discountAmount)}</span>
                            </div>
                        )}
                        <div className="total-row">
                            <span>{t.tax}</span>
                            <span>{formatCurrency(totalTax)}</span>
                        </div>
                        <div className="grand-total">
                            <span>{t.generalTotal}</span>
                            <span>{formatCurrency(total)}</span>
                        </div>
                    </div>
                </div>

                <div className="footer">
                    {companyData.name} | {companyData.website} | {companyData.phone}
                </div>
            </div>
        );
    }

    // Pro Theme Render
    if (theme === 'pro') {
        return (
            <div id={id} className={`pro-theme-container w-full max-w-[210mm] mx-auto ${config.tableDensity === 'compact' ? 'pdf-compact-mode' :
                config.tableDensity === 'ultra-compact' ? 'pdf-ultra-compact' : ''
                } `} style={containerStyles}>
                <style>{proStyles}</style>

                <div className="pdf-preview">
                    {/* Header */}
                    <div className="pdf-header">
                        <div className="header-left">
                            {config.showLogo && companyData.logo && (
                                <div className="company-logo">
                                    <img src={companyData.logo} alt="Logo" />
                                </div>
                            )}
                            <div className="company-info">
                                <div className="company-name">{companyData.name}</div>
                                <div className="company-details">
                                    <div>{companyData.address}</div>
                                    <div>{companyData.phone} | {companyData.email}</div>
                                    <div>{companyData.website}</div>
                                </div>
                            </div>
                        </div>
                        <div className="quote-info">
                            <div className="quote-title">{config.title}</div>
                            <div className="quote-meta">
                                <div className="quote-number">#{quoteData.number}</div>
                                <div>{formatDate(quoteData.date, currentLocale)}</div>
                                <div>{t.validUntil}: {formatDate(quoteData.validUntil, currentLocale)}</div>
                            </div>
                        </div>
                    </div>

                    {/* Customer Section */}
                    <div className="customer-section">
                        <div className="customer-box">
                            <div className="section-title">
                                {t.customer}
                            </div>
                            <div className="info-grid">
                                <div className="info-line">
                                    <span className="info-label">{t.company}:</span>
                                    <span className="info-value"><strong>{customerData.company}</strong></span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.authorized}:</span>
                                    <span className="info-value">{customerData.name}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.phone}:</span>
                                    <span className="info-value">{customerData.phone}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.email}:</span>
                                    <span className="info-value">{customerData.email}</span>
                                </div>
                            </div>
                        </div>
                        <div className="customer-box">
                            <div className="section-title">
                                {t.company}
                            </div>
                            <div className="info-grid">
                                <div className="info-line">
                                    <span className="info-label">{t.company}:</span>
                                    <span className="info-value"><strong>{companyData.name}</strong></span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.authorized}:</span>
                                    <span className="info-value">{companyData.authorizedPerson || '-'}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.phone}:</span>
                                    <span className="info-value">{companyData.phone}</span>
                                </div>
                                <div className="info-line">
                                    <span className="info-label">{t.email}:</span>
                                    <span className="info-value">{companyData.email}</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    {/* Items Table */}
                    <table className="pdf-items-table">
                        <thead>
                            <tr>
                                <th>#</th>
                                {config.showTableImages && <th>{t.image}</th>}
                                <th>{t.item}</th>
                                <th>{t.quantity}</th>
                                {config.showTableUnit && <th>{t.unit}</th>}
                                <th>{t.unitPrice}</th>
                                {config.showTableTax && <th>{t.tax}</th>}
                                <th>{t.total}</th>
                            </tr>
                        </thead>
                        <tbody>
                            {items.map((item, index) => (
                                <tr key={index}>
                                    <td>{index + 1}</td>
                                    {config.showTableImages && (
                                        <td>
                                            <div className="item-image">
                                                {item.image ? (
                                                    <img src={item.image} alt="" />
                                                ) : (
                                                    <span style={{ fontSize: '8px' }}>No IMG</span>
                                                )}
                                            </div>
                                        </td>
                                    )}
                                    <td>
                                        <div className="item-description">{item.name}</div>
                                        <div className="item-details">{item.description}</div>
                                    </td>
                                    <td className="item-quantity">{item.quantity}</td>
                                    {config.showTableUnit && <td className="item-unit">{item.unit}</td>}
                                    <td className="item-price">{formatCurrency(item.price)}</td>
                                    {config.showTableTax && <td className="item-tax">%{item.taxRate}</td>}
                                    <td className="item-total">{formatCurrency(item.total)}</td>
                                </tr>
                            ))}
                        </tbody>
                    </table>

                    {/* Summary */}
                    {config.showSummary && (
                        <div className="pdf-summary">
                            <div className="payment-info">
                                {config.showBankInfo && (
                                    <div className="bank-info" style={{ marginTop: 0, paddingTop: 0, borderTop: 'none' }}>
                                        <h3>{t.bankInfo}</h3>
                                        <div><strong>{bankData.bankName}</strong></div>
                                        <div>TR {bankData.iban}</div>
                                        <div>{bankData.accountHolder}</div>
                                    </div>
                                )}
                                {config.showNotes && quoteData.notes && (
                                    <div style={{ marginTop: '1rem' }}>
                                        <h3>{t.notes}</h3>
                                        <div style={{ fontSize: '0.7rem', color: '#64748b' }}>{quoteData.notes}</div>
                                    </div>
                                )}
                            </div>
                            <div className="totals-section">
                                <div className="summary-row">
                                    <span>{t.subtotal}</span>
                                    <span>{formatCurrency(subtotal)}</span>
                                </div>
                                {discount > 0 && (
                                    <div className="summary-row" style={{ color: '#ef4444' }}>
                                        <span>{t.discount}</span>
                                        <span>-{formatCurrency(discountAmount)}</span>
                                    </div>
                                )}
                                <div className="summary-row">
                                    <span>{t.tax}</span>
                                    <span>{formatCurrency(totalTax)}</span>
                                </div>
                                <div className="summary-row grand-total">
                                    <span>{t.generalTotal}</span>
                                    <span>{formatCurrency(total)}</span>
                                </div>
                            </div>
                        </div>
                    )}

                    {/* Signatures */}
                    {config.showSignatures && (
                        <div className="signature-section">
                            <div className="signature-box">
                                <div className="signature-area">
                                    {signature ? (
                                        <img src={signature} alt="Dijital İmza" style={{ maxHeight: '100%', maxWidth: '100%', objectFit: 'contain' }} />
                                    ) : companyData.signature ? (
                                        <img src={companyData.signature} alt="İmza" style={{ maxHeight: '100%', maxWidth: '100%', objectFit: 'contain' }} />
                                    ) : (
                                        <span style={{ color: '#cbd5e1', fontSize: '0.7rem' }}>{t.signature}</span>
                                    )}
                                </div>
                                <div className="signature-label">{t.deliveredBy}</div>
                            </div>
                            <div className="signature-box">
                                <div className="stamp-area" style={companyData.stamp ? { border: 'none', background: 'transparent' } : {}}>
                                    {companyData.stamp ? (
                                        <img src={companyData.stamp} alt="Kaşe" style={{ maxHeight: '100%', maxWidth: '100%', objectFit: 'contain' }} />
                                    ) : (
                                        <span style={{ color: '#cbd5e1', fontSize: '0.7rem' }}>{t.stamp} / {t.signature}</span>
                                    )}
                                </div>
                                <div className="signature-label">{t.receivedBy}</div>
                            </div>
                        </div>
                    )}

                    {/* Terms */}
                    {config.showTerms && (
                        <div className="terms-section">
                            <div className="terms-grid">
                                <div className="term-card">
                                    <h3>{t.deliveryConditions}</h3>
                                    <div className="term-content">{quoteData.deliveryTerms}</div>
                                </div>
                                <div className="term-card">
                                    <h3>{t.warrantyConditions}</h3>
                                    <div className="term-content">{quoteData.warrantyTerms}</div>
                                </div>
                            </div>
                        </div>
                    )}

                    <div className="pdf-footer">
                        <div className="footer-contact">
                            <div>{companyData.address}</div>
                            <div>{companyData.phone} | {companyData.email}</div>
                            <div>{companyData.website}</div>
                        </div>
                        <div className="footer-thanks">
                            <div className="thanks-text">Teşekkür Ederiz</div>
                            <div>Saygılarımızla, {companyData.name}</div>
                        </div>
                    </div>
                </div>
            </div>
        );
    }

    return null;
};

export default PrintableQuote;

